export type PlayerType = {
	x: number
	y: number
	radius: number
	color: string
}
export type ProjectileType = {
	x: number
	y: number
	radius: number
	color: string
	velocity: { x: number; y: number }
}
